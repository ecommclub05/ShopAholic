import { Typography } from "@mui/material";

export default function ContactPage() {     // the contact page
    return (
        <Typography variant="h2">
            Contact page
        </Typography>
    )
}